server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:~/fiapdb;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap

  h2:
    console:
      enabled:
        true
      path:
        /h2